export default function Avatar() {
  return (
    <div className="flex items-center justify-center w-12 h-12 rounded-full bg-surface-accent text-accent-contrast font-semibold text-base ring-2 ring-line select-none">
      OR
      {/*OR is just placeholder for now, replace with API call for user initials later*/}
    </div>
  );
}
