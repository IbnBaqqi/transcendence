root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "uploads", "sql", "api"]
  exclude_regex = ["_test\\.go$"]
  stop_on_error = true
  delay = 200

[misc]
  clean_on_exit = true
